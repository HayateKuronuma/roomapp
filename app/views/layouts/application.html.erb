<!DOCTYPE html>
<html>
  <head>
    <title>RoomApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.3/css/all.css">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div class="header-container">
        <div class="header-left-nav">
          <a href="/">
            <img src="/logo.png">
          </a>
          <form action="">
            <div class="header-serch">
              <input type="text" name="area" placeholder="エリア" class="header-form">
              <i class="fas fa-map-marker-alt"></i>
              </div>
              <div class="header-serch">
              <input type="text" name="keyword" placeholder="キーワード" class="header-form">
              <i class="fas fa-search"></i>
              </div>
          </form>
        </div>
        
        <% if session[:user_id] %>
          <div class="header-right-nav">
            <p class="current-user-name"><%= @current_user.name %></p>
            <div class="current-user-img">
              <img src="<%= "/#{@current_user.image_name}" %>" id="current-user-img">
              <ul class="header-nav-menu" id="nav-menu">
                <li>
                  <%= link_to("ルーム登録", "") %>
                </li>
                <li>
                  <%= link_to("予約済みルーム一覧", "") %>
                </li>
                <li>
                  <%= link_to("登録済みルーム一覧", "") %>
                </li>
                <li>
                  <%= link_to("設定", "/users/account") %>
                </li>
                <li>
                  <%= link_to("ログアウト", "/logout", {method: :post}) %>
                </li>
              </ul>
            </div>
          </div>
        <% else %>
          <div class="header-right-nav">
            <div class="btn-wrapper">
              <%= link_to("ログイン", "/users/signin", class: "btn signin-btn") %>
              <%= link_to("登録する", "/users/signup", class: "btn to-signup-btn") %>
            </div>
          </div>
        <% end %>
      </div>
    </header>
    
    <main>
      <%= yield %>
    </main>
    
    <footer class="footer">
      <p>© 2020 POTEPAN.INC.</p>
    </footer>
  </body>
</html>
